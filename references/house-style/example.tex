% Minimal document using housestyle.sty. Compile with lualatex (three passes).
\documentclass[11pt]{article}
\usepackage{housestyle}

\hsslug{House style}
\hssection{Opening \& flow diagram}

\begin{document}

\hstitleblock{Documentation template}{A house style for technical documents}%
{One page geometry, one type pairing, and a kit of twelve blocks.}

\section{How a document opens}
\hslead{A short document starts at the top of page one. Anything past twelve
pages gets a title page.}

The opening paragraph is the only place a document is allowed to be general.

\begin{hsfigure}{Figure 1 / node-and-arrow flow}%
{Slate boxes are inputs and outputs, sage boxes are work an agent does,
rust boxes are deterministic checks.}
\begin{tikzpicture}[node distance=14pt]
  \node[hsnode] (a) {\hsnodetext{Input}{A brief}{goal, boundaries, done-criteria}};
  \node[hswork,right=of a] (b) {\hsnodetext{Agent}{Draft}{structure first, then prose}};
  \node[hsgate,right=of b] (c) {\hsnodetext{Gate}{Style check}{pass, or back with reasons}};
  \node[hsnode,right=of c] (d) {\hsnodetext{Output}{A PDF}{with its sources attached}};
  \draw[hsarrow] (a) -- (b); \draw[hsarrow] (b) -- (c); \draw[hsarrow] (c) -- (d);
  \draw[hsfail] (c.south) -- ++(0,-12pt) -| (b.south);
\end{tikzpicture}
\hsfailnote{A failed gate returns to the draft step, twice, then to a person}
\end{hsfigure}

\begin{hsstatrow}
  \hsstat{741}{changes merged across eight projects in 26 days}
  \hsstat{1 in 3}{stopped at least once by the box's own checks}
  \hsstat{62}{small tools, about 65,000 lines}
  \hsstat{65 W}{power supply; the models run elsewhere}
\end{hsstatrow}

% A table is a block, so it goes on the whole measure inside hsblock.
\begin{hsblock}
\hslabel{Table 1 / what the three node kinds mean}\par\vspace{8pt}
\begin{tabular}{L{0.26\linewidth} L{0.30\linewidth} L{0.30\linewidth}}
\hstoprule
\hshead{Kind} & \hshead{How it is drawn} & \hsaccenthead{Use it for} \\
\hline
\texttt{hsnode} & Slate rule, slate-tint fill & An input or an output \\
\texttt{hswork} & Sage rule, sage-tint fill & Work an agent does \\
\texttt{hsgate} & Accent rule, accent-tint fill & A check that passes or fails \\
\hline
\end{tabular}
\end{hsblock}

\hsclaim{An agent that writes code is easy to get now. One you can stop watching
is harder.}{One per document}

\begin{hscallout}{What this does not mean}
The spec can be wrong, the test can miss a case, and the measurement is four
weeks old.
\end{hscallout}

\hsprovenance{Counts read off the box on 21 September 2026 from
\textasciitilde/.cc/state/land/queue.log, deploy-only jobs excluded.}

\end{document}
